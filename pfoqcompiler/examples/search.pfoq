decl search(a,b){
    if(|a|>1)then{
        qcase(a[|a|/2-1],a[|a|/2]) of {
	    00 -> call search((a)^+ -[0], b);,
	    01 -> b[0] *= NOT;,
	    10 -> call search((a)^- -[-1], b);,
	    11 -> skip;} }
    else{skip;} }
::
define q r;
::
call search(q,r);
