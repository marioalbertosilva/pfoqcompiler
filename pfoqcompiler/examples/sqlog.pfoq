decl f(a,b){
    q[0] *= Ph(|q|);
    call f((a)^+,b);
    call g(a,b-[0]);}
    
decl g(a,b){
    qcase(a[|a|/2])of{
        0->call g((a)^-,b);,
        1->b[0]*=NOT;
    }
}
::
define q r;
::
call f(q,r);
